# Reel Shelf

A small Node.js movie collection tracker. Add a title, rate it from 1 to 10, and keep a short note. Entries are stored in the browser's local storage.

## Run locally

```bash
npm start
```

Then open `http://localhost:3000`.

## Test

```bash
npm test
```
