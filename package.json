{
  "name": "movie-collection",
  "version": "1.0.0",
  "private": true,
  "description": "A small, responsive movie collection tracker.",
  "scripts": {
    "start": "node server.js",
    "test": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
